#include <stdio.h>
/**
 * main - prints its name
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 (success)
 */
int main(int argc, int *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
