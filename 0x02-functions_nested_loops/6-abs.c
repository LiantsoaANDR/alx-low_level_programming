#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @n: is the number to compute
 *
 * Return: an int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
